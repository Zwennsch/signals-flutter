import 'package:flutter/material.dart';

class HttpScreen extends StatelessWidget {
  const HttpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Http"),),);
  }
}